namespace GameBrains.Actuators.Motion.Navigation.SearchAlgorithms.SingleUpdateSearch
{
	// A single update search can Succeed (find a path), or Fail (not find a path).
	public enum SearchResults
    {
		Success, 
		Failure
    }
}