namespace GameBrains.Actuators.Motion.Navigation.SearchAlgorithms.OneCyclePerUpdateSearch
{
    // Currently supported one cycle per update search types.
	public enum SearchTypes
    {
        AStar, 
        Dijkstra
    }
}