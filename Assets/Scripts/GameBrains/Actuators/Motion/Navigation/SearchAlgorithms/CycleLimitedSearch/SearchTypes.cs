namespace GameBrains.Actuators.Motion.Navigation.SearchAlgorithms.CycleLimitedSearch
{
    // Currently supported cycle limited search types.
	public enum SearchTypes
    {
        AStar, 
        Dijkstra
    }
}